package com.hospital.backend.entity;

public enum NotificationType {
    RENDEZ_VOUS,
    DOCUMENT,
    FACTURATION,
    PAIEMENT, // Ajouté pour correspondre à ton interface React (CreditCard icon)
    SYSTEME
}